{% extends 'base_back.html.twig' %}

{% block title %}Emplacement{% endblock %}

{% block main %}
    <h1>Emplacement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ emplacement.id }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ emplacement.lieu }}</td>
            </tr>
            <tr>
                <th>Capacite</th>
                <td>{{ emplacement.capacite }}</td>
            </tr>
            <tr>
                <th>stock</th>
                <td>{{ emplacement.stock }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('emplacement_index') }}"><button type="button" class="btn btn-theme02"><i class="fa fa-backward"></i> Back</button></a>

    <a href="{{ path('emplacement_edit', {'id': emplacement.id}) }}"><button type="button" class="btn btn-theme02"><i class="fa fa-edit"></i> Modifier</button></a>

    {{ include('emplacement/_delete_form.html.twig') }}
{% endblock %}
