{% extends 'base_back.html.twig' %}

{% block title %}Sujet{% endblock %}

{% block main %}
    <h1>Sujet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sujet.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ sujet.Date ? sujet.Date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ sujet.titre }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ sujet.contenu }}</td>
            </tr><tr>
                <th>NbReponses</th>
                <td>{{ sujet.nbReponses }}</td>
            </tr><tr>
                <th>NbVues</th>
                <td>{{ sujet.nbVues }}</td>
            </tr><tr>
                <th>User</th>
                <td>{{ sujet.IdUser.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sujet_index') }}">back to list</a>

    <a href="{{ path('sujet_edit', {'id': sujet.id}) }}">edit</a>

    {{ include('sujet/_delete_form.html.twig') }}
{% endblock %}
